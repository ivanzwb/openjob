import type { CapabilityDeclaration } from '@core/plugins/types';

/** 插入点 E：产品岗位内嵌的能力（表格案例分析；portfolio-review 尚无宿主实现）。 */
export const capabilities: CapabilityDeclaration[] = [{ id: 'analytics-case' }];
