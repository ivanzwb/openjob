{
  "name": "@openjob/plugins",
  "version": "0.0.0",
  "description": "官方岗位包聚合目录：每个岗位是独立的 workspace 子项目（plugins/*），随 release 单独分发（纯数据，不进基础包）",
  "private": true,
  "type": "module",
  "//": "本包是聚合容器，npm 版本号恒为 0.0.0 且无意义。真正的发布版本写在各岗位子项目自己的 manifest 里（plugins/*/package.json，与 index.ts 里的 *_ROLE_PACK_VERSION 保持一致），由 scripts/pack-plugins.mjs 各自打成签名包。",
  "dependencies": {
    "@openjob/core": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
