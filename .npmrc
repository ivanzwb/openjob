# @types/* 必须提升到工作区根的 node_modules。
#
# 第三方包自带的 .d.ts 会 `import ... from 'react'` 这类裸引用，TypeScript 解析它们时
# 是从该包在 .pnpm 虚拟目录里的位置往上走，走到工作区根就到头了——而 @types/react 是
# desktop 的 devDependency，只躺在 desktop/node_modules 里，这条路径上找不到。
# 症状很不像依赖问题：react-qr-code 的 QRCodeSVG 报 "cannot be used as a JSX component"。
#
# 单包时期没这个问题，因为那时 @types/react 就在根 node_modules 上。
public-hoist-pattern[]=@types/*
